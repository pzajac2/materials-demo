<?php

use Application\Form\UnitOfMeasureForm;
use Zend\Form\Form;

/** @var Form $form */

$form->prepare();
?>

<h2>
<?php if ($newEntity): ?>
    Dodaj jednostkę miary
<?php else: ?>
    Edytuj jednostkę
<?php endif; ?>
</h2>

<div class="row">
    <div class="col-sm-12">

        <?= $this->form()->openTag($form) ?>

        <?= $this->bootstrapRow($form->get(UnitOfMeasureForm::NAME)) ?>

        <?= $this->bootstrapRow($form->get(UnitOfMeasureForm::SHORT_NAME)) ?>

        <?= $this->bootstrapRow($form->get(UnitOfMeasureForm::SUBMIT)) ?>

        <?= $this->form()->closeTag($form); ?>
    </div>
</div>

